<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Carpeta pública: /storage/emulated/0/Download/Encrypt_Android -->
    <external-path
        name="public_download"
        path="Download/Encrypt_Android/" />

    <!-- Archivos en storage interno privado: context.filesDir -->
    <files-path
        name="private_files"
        path="." />

    <!-- Archivos en externalFilesDir (privado pero visible): context.getExternalFilesDir(...) -->
    <external-files-path
        name="external_app_files"
        path="." />

</paths>
